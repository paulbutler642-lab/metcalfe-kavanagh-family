const cfg=window.__APP_CONFIG__||{};const client=cfg.SUPABASE_URL&&cfg.SUPABASE_PUBLISHABLE_KEY?window.__SUPABASE_CLIENT__:null;
export async function trackVisit(){if(!client||new URLSearchParams(location.search).get('view')==='admin'||location.pathname.startsWith('/admin'))return;const home=(new URLSearchParams(location.search).get('view')||'home')==='home';if(sessionStorage.getItem('family_archive_visit_recorded')){if(home)await showCounter();return}const visitorId=getId(localStorage,'family_archive_visitor'),sessionId=getId(sessionStorage,'family_archive_session'),path=location.pathname+location.search;let referrer='Direct visit';try{if(document.referrer){const u=new URL(document.referrer);referrer=u.origin===location.origin?'Another page on this site':u.hostname}}catch{}let geo={};try{const r=await fetch('/api/visitor-location',{cache:'no-store'});if(r.ok)geo=await r.json()}catch{}const {error}=await client.from('site_visits').insert({visitor_id:visitorId,session_id:sessionId,path,referrer,user_agent:navigator.userAgent.slice(0,500),country:geo.country||null,region:geo.region||null,city:geo.city||null});if(!error)sessionStorage.setItem('family_archive_visit_recorded','yes');if(home)await showCounter()}
async function showCounter(){const {data}=await client.from('site_stats').select('stat_value').eq('stat_key','visits').maybeSingle(),hero=document.querySelector('.hero');if(!hero||!data)return;const badge=document.createElement('div');badge.className='visit-ticker';badge.setAttribute('aria-label',`${data.stat_value} site visits`);badge.innerHTML=`<span>Site visits</span><strong>${Number(data.stat_value).toLocaleString('en-IE')}</strong>`;hero.append(badge)}
export async function mountAdminAnalytics(container,{db}){container.className='card visitor-analytics';container.innerHTML='<p class="muted">Loading visitor activity…</p>';const [{data:visits,error},{data:stat}]=await Promise.all([db.from('site_visits').select('*').order('visited_at',{ascending:false}).limit(1000),db.from('site_stats').select('stat_value').eq('stat_key','visits').maybeSingle()]);if(error){container.innerHTML=`<div class="status error">${esc(error.message)}</div>`;return}const rows=visits||[],unique=new Set(rows.map(x=>x.visitor_id)).size,located=rows.filter(v=>v.country||v.region||v.city),locationCounts=new Map();for(const v of located){const key=locationLabel(v);locationCounts.set(key,(locationCounts.get(key)||0)+1)}const topLocations=[...locationCounts.entries()].sort((a,b)=>b[1]-a[1]).slice(0,8);container.innerHTML=`<div class="analytics-head"><div><span class="source-type">Private administrator statistics</span><h2>Site Visitors</h2><p class="muted">Privacy-safe activity with approximate network location. Unique visitors are estimated per browser; full IP addresses and precise coordinates are not collected.</p></div></div><div class="analytics-summary"><div><strong>${Number(stat?.stat_value||0).toLocaleString('en-IE')}</strong><span>Total visits</span></div><div><strong>${unique.toLocaleString('en-IE')}</strong><span>Unique browsers*</span></div><div><strong>${rows.length.toLocaleString('en-IE')}</strong><span>Recent visits shown</span></div></div>${topLocations.length?`<div class="analytics-locations"><h3>Visitors by location</h3><p class="muted">Approximate location based on the visitor's internet connection.</p><div class="location-chips">${topLocations.map(([name,count])=>`<span><strong>${esc(name)}</strong> ${count}</span>`).join('')}</div></div>`:''}<div class="analytics-list">${rows.slice(0,20).map(v=>`<article><div><strong>Visitor ${esc(v.visitor_id.slice(0,8))}</strong><small>${esc(device(v.user_agent))} • ${esc(displayReferrer(v.referrer))}</small>${(v.country||v.region||v.city)?`<small>Approx. location: ${esc(locationLabel(v))}</small>`:''}</div><div><strong>${new Date(v.visited_at).toLocaleString('en-IE',{dateStyle:'medium',timeStyle:'short'})}</strong><small>Entered on ${esc(v.path)}</small></div></article>`).join('')||'<p class="muted">No visits have been recorded yet.</p>'}</div><small class="muted">*A person may be counted again if they use another device or clear browser storage. Location is approximate and can be affected by mobile networks, VPNs and internet-provider routing.</small>`}
function locationLabel(v){const countryName=country(v.country),parts=[];if(v.city)parts.push(v.city);if(v.region&&!parts.includes(v.region))parts.push(v.region);if(countryName)parts.push(countryName);return parts.join(', ')||'Unknown'}function country(c){return ({IE:'Ireland',GB:'United Kingdom',US:'United States',CA:'Canada',AU:'Australia',NZ:'New Zealand',BR:'Brazil',DE:'Germany',FR:'France',ES:'Spain',IT:'Italy',PT:'Portugal'})[c]||c||''}function displayReferrer(v){return v==='Another family page'?'Another page on this site':(v||'Direct visit')}function getId(storage,key){let id=storage.getItem(key);if(!id){id=crypto.randomUUID();storage.setItem(key,id)}return id}function device(ua=''){if(/Android/i.test(ua))return'Android phone/tablet';if(/iPhone|iPad/i.test(ua))return'iPhone/iPad';if(/Windows/i.test(ua))return'Windows computer';if(/Macintosh/i.test(ua))return'Mac';return'Web browser'}function esc(v){return String(v??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]))}
